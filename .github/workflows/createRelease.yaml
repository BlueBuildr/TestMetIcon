name: CreateRelease
on:
  pull_request:
    types: [closed]

jobs:
  create-release:
    if: github.event.pull_request.merged == true
    uses: BlueBuildr/.github/.github/workflows/createReleaseV7.yaml@main
